# Skills CLI runtime (restored on session start)
.agents/

# User-local launcher overrides
claude.local.sh

# MCP server secrets (API keys, tokens)
.mcp.env

# Template state (managed by update-from-template.sh)
.claude/.template-checksums
.claude/.template-version
